[settings]
throttle-output = 0
throttle-output-for = 10
throttle-input-for = 30

[colors]
blackDark = ${xrdb:polybar.color0:#000000}
blackLight = ${xrdb:polybar.color8:#000000}
redDark = ${xrdb:polybar.color1:#000000}
redLight = ${xrdb:polybar.color9:#000000}
greenDark = ${xrdb:polybar.color2:#000000}
greenLight = ${xrdb:polybar.color10:#000000}
yellowDark = ${xrdb:polybar.color3:#000000}
yellowLight = ${xrdb:polybar.color11:#000000}
blueDark = ${xrdb:polybar.color4:#000000}
blueLight = ${xrdb:polybar.color12:#000000}
magentaDark = ${xrdb:polybar.color5:#000000}
magentaLight = ${xrdb:polybar.color13:#000000}
cyanDark = ${xrdb:polybar.color6:#000000}
cyanLight = ${xrdb:polybar.color14:#000000}
whiteDark = ${xrdb:polybar.color7:#000000}
whiteLight = ${xrdb:polybar.color15:#000000}

background = ${self.blackDark}
background-alt = ${self.whiteDark}
foreground = ${self.whiteDark}
foreground-alt = ${self.blackDark}
alert = ${self.redDark}

[bar/top]
width = 100%
module-margin = 1
padding = 1

border-top-size = 12
border-bottom-size = 12
border-top-color = ${self.background}
border-bottom-color = ${self.background}

modules-left = i3
modules-center = 
modules-right = keyboard wireless-network backlight pulseaudio battery date

font-0 = "Monospace:size=10:antialias=false;2"
font-1 = "FontAwesome:size=10:style=solid;2"
font-2 = "FontAwesomeBrands:size=10:style=solid;2"

locale = en_US.UTF-8

tray-position = right
tray-padding = 5

cursor-click = pointer
cursor-scroll = ns-resize

foreground = ${colors.foreground}
background = ${colors.background}

[module/backlight]
type = internal/backlight
card = amdgpu_bl0

format = <label>

format-prefix = 
format-prefix-background = ${colors.yellowLight}
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-padding = 1

label = %percentage:3%%
label-background = ${colors.background-alt}
label-foreground = ${colors.foreground-alt}
label-padding = 1

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 78

poll-interval = 5

label-full = %percentage:3%%
label-full-background = ${colors.background-alt}
label-full-foreground = ${colors.foreground-alt}
label-full-padding = 1

format-full-prefix = 
format-full-prefix-background = ${colors.magentaLight}
format-full-prefix-foreground = ${colors.foreground-alt}
format-full-prefix-padding = 1

format-discharging = <label-discharging>

label-discharging = %percentage:3%%
label-discharging-background = ${colors.background-alt}
label-discharging-foreground = ${colors.foreground-alt}
label-discharging-padding = 1

format-discharging-prefix = 
format-discharging-prefix-background = ${colors.magentaLight}
format-discharging-prefix-foreground = ${colors.foreground-alt}
format-discharging-prefix-padding = 1

format-charging = <label-charging>

label-charging = %percentage:3%%
label-charging-background = ${colors.background-alt}
label-charging-foreground = ${colors.foreground-alt}
label-charging-padding = 1

format-charging-prefix = 
format-charging-prefix-background = ${colors.magentaLight}
format-charging-prefix-foreground = ${colors.foreground-alt}
format-charging-prefix-padding = 1

[module/date]
type = internal/date

interval = 5.0

time = %H:%M
time-alt = %b %d, %H:%M

format = <label>

format-prefix = 
format-prefix-background = ${colors.greenLight}
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-padding = 1

label = %time%
label-background = ${colors.background-alt}
label-foreground = ${colors.foreground-alt}
label-padding = 1

[module/wireless-network]
type = internal/network
interface = wlp2s0

interval = 3

format-connected = <label-connected>

label-connected = %essid%
label-connected-background = ${colors.background-alt}
label-connected-foreground = ${colors.foreground-alt}
label-connected-padding = 1

format-connected-prefix = 
format-connected-prefix-background = ${colors.redLight}
format-connected-prefix-foreground = ${colors.foreground-alt}
format-connected-prefix-padding = 1

[module/i3]
type = internal/i3
index-sort = true
strip-wsnumbers = true

; Only show workspaces on the same output as the bar
; pin-workspaces = true

format = <label-state> <label-mode>

padding = 1

label-mode-background = ${colors.alert}
label-mode-foreground = ${colors.foreground}
label-mode-padding = ${self.padding}

label-focused = %name%
label-focused-background = ${colors.blueLight}
label-focused-foreground = ${colors.foreground-alt}
label-focused-padding = ${self.padding}

label-unfocused = %name%
label-unfocused-background = ${colors.background-alt}
label-unfocused-foreground = ${colors.foreground-alt}
label-unfocused-padding = ${self.padding}

label-visible = %name%
label-visible-background = ${colors.background-alt}
label-visible-foreground = ${colors.foreground-alt}
label-visible-padding = ${self.padding}

label-urgent = %name%
label-urgent-background = ${colors.background-alt}
label-urgent-foreground = ${colors.foreground-alt}
label-urgent-padding = ${self.padding}

[module/pulseaudio]
type = internal/pulseaudio

use-ui-max = true
interval = 5

label-volume = %percentage:3%%
label-volume-background = ${colors.background-alt}
label-volume-foreground = ${colors.foreground-alt}
label-volume-padding = 1

format-volume = <label-volume>

format-volume-prefix = 
format-volume-prefix-background = ${colors.blueLight}
format-volume-prefix-foreground = ${colors.foreground-alt}
format-volume-prefix-padding = 1

label-muted = %percentage:3%%
label-muted-background = ${colors.background-alt}
label-muted-foreground = ${colors.foreground-alt}
label-muted-padding = 1

format-muted = <label-muted>

format-muted-prefix = 
format-muted-prefix-background = ${colors.blueLight}
format-muted-prefix-foreground = ${colors.foreground-alt}
format-muted-prefix-padding = 1

[module/keyboard]
type = internal/xkeyboard

format = <label-layout>

label-layout = %layout%
label-layout-background = ${colors.background-alt}
label-layout-foreground = ${colors.foreground-alt}
label-layout-padding = 1

format-prefix = 
format-prefix-background = ${colors.cyanLight}
format-prefix-foreground = ${colors.foreground-alt}
format-prefix-padding = 1
