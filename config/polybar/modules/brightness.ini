[module/brightness]

type = custom/script

exec = "$HOME/.config/polybar/brightness.sh"

scroll-up = "$HOME/.config/polybar/brightness.sh up"
scroll-down = "$HOME/.config/polybar/brightness.sh down"

interval = 1

format-prefix = 
format-prefix-padding-right = 1
